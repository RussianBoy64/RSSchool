export default async function createQuizPage(lang) {
    const questions = document.createElement('section')
}

async function createQuestions(lang) {
    const questions = document.createElement('section')
    
    questions.className = 'quiz__questions'

}